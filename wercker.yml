build:
  box:
    id: sso2712/blog
  steps:
    - setup-go-workspace
    - script:
        name: build
        code: |
          echo -e "machine github.com\n login $GH_USER_TOKEN" >> ~/.netrc
          ./setup.sh
          ./deploy.sh
